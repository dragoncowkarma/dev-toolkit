# Agent Skills Directory

This directory contains reusable skill definitions for AI agents.

Skills are shared capabilities that any agent can use, regardless of vendor.

## Creating a New Skill

1. Create a new markdown file: `<skill_name>.md`
2. Document the skill's purpose, inputs, and expected outputs.
3. Reference it in `AGENTS.md` or `.agents/rules/` as needed.
